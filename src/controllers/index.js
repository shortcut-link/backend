const account = require('./account');
const session = require('./session');
const link = require('./link');
const adminLink = require('./admin/link');

module.exports = { account, session, link, adminLink };
